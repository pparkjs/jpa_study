spring:
#  datasource:
#    url: jdbc:h2:mem:test 별도에 설정 없으면 스프링부트는 메모리모드로 돌림
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create-drop
#    properties:
#      hibernate:
#        show_sql: true  sysout을 통해 보는 것
#        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.orm.jdbc.bind: trace # 쿼리 파라미터 로그 남기기
